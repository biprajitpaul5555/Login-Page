<%- include('partials/header'); -%>

<div class="jumbotron text-center">
    <div class="container">
        <i class="fas fa-key fa-6x"></i>
        <h1 class="display-3"><%= heading %></h1>
        <p class="secret-text"><%= para %></p>
        <hr />
        <% if(title=="Success"){ %>
            <a class="btn btn-dark btn-lg" href="/logout" role="button">Log Out</a>
        <% } else{ %>
            <a class="btn btn-dark btn-lg" href="/" role="button">Log In</a>
            <a class="btn btn-dark btn-lg" href="/signup" role="button">Sign Up</a>
        <% } %>
    </div>
</div>

<%- include('partials/footer'); -%>